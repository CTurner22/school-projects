#include "SolutionAlgorithm.h"

solutionFinder::solutionFinder(const string &fileName) {
    input = inputReader(fileName);
    solutionLogic();
}

void solutionFinder::solutionLogic() {
    /*
     * todo: Start the actual algorithm here
     *
     */
}

void solutionFinder::outputToFile(const string &fileName) {
    // todo: write the solution to file
}
